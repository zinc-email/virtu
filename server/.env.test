# Loaded by Bun when NODE_ENV=test (`bun test` sets it): host-side test-tier
# overrides. Committed on purpose — the int tier mints login codes freely
# from one machine, and the production ceiling (500/hour) would turn the
# fifth full run of the hour into a wall of 429s.
TRANSACTIONAL_MAIL_HOURLY_MAX=0
